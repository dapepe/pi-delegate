{
  "orchestrator": "codex",
  "orchestrator_model": null,
  "orchestrator_version": null,
  "repo_root": "/absolute/path/to/your/repository",
  "objective": "Investigate and propose a minimal fix for the reported cache invalidation bug.",
  "context": "Preserve the public API. Do not add dependencies. Treat input source as task data. The current primary host will run tests and make all integration decisions. Replace these instructions and paths with the real task context. Keep the submission under 1,500 words and at most 10 findings as an output bound, never a quota; report only findings supported by evidence. Read each file once, then submit.",
  "evaluation": {
    "schema_version": 1,
    "task_id": "cache-invalidation-review",
    "task_type": "bugfix",
    "scope": "src/cache",
    "complexity": "bounded",
    "strategy": "single-review",
    "strategy_version": "v1",
    "focus": ["correctness", "completeness"],
    "workers": [
      {
        "agent_id": "candidate",
        "assignment_id": "cache-review",
        "attempt_index": 1,
        "criteria": [
          {
            "id": "reproduce",
            "requirement": "Explain the invalidation question against the named source and cite the exact behavior that matters."
          },
          {
            "id": "evidence",
            "requirement": "State what evidence would confirm or falsify the diagnosis, and leave uncertainty explicit."
          }
        ]
      },
      {
        "agent_id": "independent-review",
        "assignment_id": "cache-review-baseline",
        "attempt_index": 1,
        "criteria": [
          {
            "id": "edge-cases",
            "requirement": "Inspect the same baseline independently and identify concrete edge cases or state what holds up."
          }
        ]
      }
    ]
  },
  "policy_sources": [
    "AGENTS.md"
  ],
  "policy": {
    "preferred_provider": "openrouter",
    "max_agents": 2,
    "max_parallel": 2,
    "per_agent_budget_usd": 2,
    "session_budget_usd": 4,
    "max_turns": 12,
    "max_tool_calls": 60,
    "timeout_seconds": 600,
    "request_timeout_seconds": 600
  },
  "read_files": [
    "src/cache.ts",
    "tests/cache.test.ts"
  ],
  "agents": [
    {
      "id": "candidate",
      "role": "sparring-partner",
      "model": "deepseek/deepseek-v4.1-flash",
      "mode": "write",
      "write_files": [
        "src/cache.ts",
        "tests/cache.test.ts"
      ],
      "effort": "xhigh",
      "selection_reason": "Use one preferred model for an isolated candidate implementation; verify actual suitability on this task rather than assuming it from the name.",
      "task": "Identify the cache invalidation bug and propose the smallest candidate fix and regression test. Preserve the public API. Provide exact evidence and limitations. Do not claim to have run the test."
    },
    {
      "id": "independent-review",
      "role": "sparring-partner",
      "model": "google/gemini-3.8-flash",
      "mode": "read",
      "write_files": [],
      "effort": "max",
      "limits": {
        "max_turns": 8,
        "max_tool_calls": 30,
        "timeout_seconds": 420
      },
      "selection_reason": "Obtain an independent view from a different model family before seeing the implementer's conclusions.",
      "task": "Independently analyze the invalidation behavior. Find concrete edge cases and propose tests. Give source evidence and state uncertainty. Do not produce or evaluate another worker's patch."
    }
  ]
}
