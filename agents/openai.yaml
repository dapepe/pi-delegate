interface:
  display_name: "pi"
  short_description: "Independent sparring and isolated model work"
  default_prompt: "Use $pi for an independent model perspective or an isolated candidate edit, then verify the evidence and report usefulness and cost."
policy:
  allow_implicit_invocation: true
